# Code Changes — Apply Manually

Edits could not be applied automatically (permission denied). Run:

```bash
cd /Users/christopherhastings/recorder && bash apply_changes.sh
```

Or apply the edits manually per the summary below.
